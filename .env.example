# Database (PostgreSQL)
# Docker Compose internal networking (backend service):
DATABASE_URL="postgresql://sentinel_user:sentinel_pass@postgres:5432/sentinel_db?schema=public"
# Local development (connecting from host to port 5433):
# DATABASE_URL="postgresql://sentinel_user:sentinel_pass@localhost:5433/sentinel_db?schema=public"

# Application
PORT=4000
NODE_ENV=development
